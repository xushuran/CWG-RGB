# CWG-RGB benchmark environment
# Python: 3.10.11
#
# For the reported GPU setup, install the CUDA 12.6 PyTorch wheels first, for example:
# pip install torch==2.11.0 torchvision==0.26.0 torchaudio==2.11.0 --index-url https://download.pytorch.org/whl/cu126
#
# Then install the remaining dependencies:
# pip install -r requirements.txt

ultralytics==8.4.37

# Core scientific and image-processing dependencies used by Ultralytics YOLO
# and the CWG-RGB utility scripts.
numpy>=1.23.0
opencv-python>=4.6.0
Pillow>=7.1.2
PyYAML>=5.3.1
pandas>=1.1.4
matplotlib>=3.3.0
scipy>=1.4.1
tqdm>=4.64.0

# PyTorch is listed for CPU/default pip environments. For CUDA reproduction,
# prefer the command above so the correct CUDA wheel is installed.
torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0
